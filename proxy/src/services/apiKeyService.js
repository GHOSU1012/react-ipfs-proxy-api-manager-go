import axios from 'axios'

export const log = async (request) => {
    //axios.post('??')
}

export const checkIsValid = async (request) => {
    //const { isValid } = await axios.get('??')
    //return isValid
    return true
}